# data/

Generated and seed data, not authored by hand.

- `simulation_runs/` — output of `backend/simulation/run_simulation.py` (Phase 1). CSV files are gitignored; regenerate locally by running the simulator.
- Knowledge-base seed data lands here starting Phase 2 — see CLAUDE.md §11.
